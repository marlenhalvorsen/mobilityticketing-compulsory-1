## Assumptions

A route may visit the same stop more than once. Therefore(route_id, stop_sequence) is used as a composite primary key for route_stops.

This allows the same stop to appear multiple times on a route at different positions.
